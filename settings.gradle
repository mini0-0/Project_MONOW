pluginManagement {
	repositories {
		maven { url = 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'monow'

include 'api'
include 'domain'
include 'global'
include 'batch'